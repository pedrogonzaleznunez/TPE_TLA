// Cubre: sensor DHT11, multiples metodos sobre un componente
hardware {
    dht11 climate_sensor on 4;
}
routine {
    var temp = climate_sensor.read_temperature();
    var hum = climate_sensor.read_humidity();
}
