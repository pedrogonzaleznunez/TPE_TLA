// Cubre: repeat N times con cuerpo de sentencias
hardware {
    led blink_led on 13;
}
routine {
    repeat 5 times {
        blink_led.turn_on();
        wait 100ms;
        blink_led.turn_off();
    }
}
