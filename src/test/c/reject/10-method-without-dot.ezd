// Llamada a metodo sin punto — error sintactico puro
// STAGE II: rechaza 
// Motivo: la sintaxis de llamada a metodo es: objeto.metodo(args)
hardware {
    led alert_light on 13;
}
routine {
    alert_light turn_on();
}
